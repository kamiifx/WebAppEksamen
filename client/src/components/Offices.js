import React from 'react';
import styled from 'styled-components';
import {Header, Container, ArticleBlock, ArticleIntro, BoxButton, FlexBox} from '../styled/Styled';


function Offices(){

    return(
        <div>
            <Header>
                <h2>Kontorer </h2>
            </Header>

            <Container>
                <h2>Fredrikstad(8 Kontorer)</h2>
                <FlexBox>

                </FlexBox>
            </Container>
        </div>
    )
}

export default Offices;